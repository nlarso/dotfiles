#navigation
alias fff="cd ~/Code "

#rails/docker
alias bake="bundle exec rake "
alias dails='docker-compose run --rm web rails'
alias dake='docker-compose run --rm web rake'
alias dash='docker-compose run --rm web bash -i -c'
alias dbsetup='docker-compose run --rm web bin/setup'
alias dcr='docker-compose run --rm web'
alias rmpid='rm tmp/pids/server.pid'

# color ls
colorflag="-G"
alias ls="command ls ${colorflag}"
alias l="ls -lF ${colorflag}" # all files, in long format
alias la="ls -laF ${colorflag}" # all files inc dotfiles, in long format
alias lsd='ls -lF ${colorflag} | grep "^d"' # only directories

# misc
alias sudo='sudo ' # allow aliases to be sudo'ed
